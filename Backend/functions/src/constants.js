const ROLES = new Set(["customer", "owner"]);
const PLATFORMS = new Set(["android", "ios", "web"]);
const SERVICE_CATEGORIES = new Set(["quick", "interior", "complete", "premium"]);

module.exports = {ROLES, PLATFORMS, SERVICE_CATEGORIES};
